--[[ 
    SCRIPT VISUAL PARA DELTA EXECUTOR
    Estilo: Bordes Circulares + Spawner Fruits (Visual)
--]]

local player = game.Players.LocalPlayer
local coreGui = game:GetService("CoreGui")

-- Evitar que el script se duplique si lo ejecutas varias veces
if coreGui:FindFirstChild("VisualModMenu") then
    coreGui:FindFirstChild("VisualModMenu"):Destroy()
end

-- Contenedor Principal (ScreenGui)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VisualModMenu"
screenGui.Parent = coreGui
screenGui.IgnoreGuiInset = true -- Para que cubra toda la pantalla en Android

-- Marco Principal (El Cuadrado con bordes de círculo)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 300, 0, 350)
mainFrame.Position = UDim2.new(0.5, -150, 0.5, -175)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- BORDES REDONDEADOS (UICorner)
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 25) -- Esto hace el efecto circular en las esquinas
mainCorner.Parent = mainFrame

-- Título: SPAWNER FRUITS
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 60)
title.Text = "SPAWNER FRUITS"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 22
title.Parent = mainFrame

-- Cuadro para escribir el texto (TextBox)
local inputField = Instance.new("TextBox")
inputField.Size = UDim2.new(0.85, 0, 0, 50)
inputField.Position = UDim2.new(0.075, 0, 0.3, 0)
inputField.PlaceholderText = "LEOPARD ( WEST )..."
inputField.Text = ""
inputField.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
inputField.TextColor3 = Color3.new(1, 1, 1)
inputField.Font = Enum.Font.Gotham
inputField.TextSize = 16
inputField.Parent = mainFrame

local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 12)
inputCorner.Parent = inputField

-- Botón SPAWNER
local spawnButton = Instance.new("TextButton")
spawnButton.Size = UDim2.new(0.85, 0, 0, 55)
spawnButton.Position = UDim2.new(0.075, 0, 0.55, 0)
spawnButton.Text = "SPAWNER"
spawnButton.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
spawnButton.TextColor3 = Color3.new(1, 1, 1)
spawnButton.Font = Enum.Font.GothamBold
spawnButton.TextSize = 20
spawnButton.Parent = mainFrame

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 15)
btnCorner.Parent = spawnButton

-- Log de Estado (Texto abajo)
local statusLog = Instance.new("TextLabel")
statusLog.Size = UDim2.new(0.85, 0, 0, 40)
statusLog.Position = UDim2.new(0.075, 0, 0.75, 0)
statusLog.Text = "Sistema listo para ejecutar"
statusLog.TextColor3 = Color3.fromRGB(150, 150, 150)
statusLog.BackgroundTransparency = 1
statusLog.Font = Enum.Font.Code
statusLog.TextSize = 14
statusLog.Parent = mainFrame

-- Lógica Visual del LocalScript
spawnButton.MouseButton1Click:Connect(function()
    local fruit = inputField.Text
    if fruit ~= "" then
        spawnButton.Text = "CARGANDO..."
        statusLog.Text = "Inyectando bypass en servidor..."
        task.wait(1.5)
        statusLog.TextColor3 = Color3.fromRGB(0, 255, 255)
        statusLog.Text = "Fruta detectada: " .. fruit
        task.wait(1.2)
        statusLog.TextColor3 = Color3.fromRGB(0, 255, 0)
        statusLog.Text = "¡EXITO! FRUTA SPAWNEADA (VISUAL)"
        spawnButton.Text = "TERMINADO"
        task.wait(2)
        spawnButton.Text = "SPAWNER"
        statusLog.TextColor3 = Color3.fromRGB(150, 150, 150)
        statusLog.Text = "Esperando nueva orden..."
    else
        statusLog.Text = "Error: Debes escribir una fruta"
        statusLog.TextColor3 = Color3.fromRGB(255, 0, 0)
    end
end)

-- Botón para cerrar (X)
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -40, 0, 10)
closeBtn.Text = "X"
closeBtn.BackgroundColor3 = Color3.new(0.8, 0, 0)
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0) -- X Circular
closeCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

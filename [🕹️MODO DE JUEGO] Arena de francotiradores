-- VARIABLES DE SESIÓN Y SEGURIDAD
local FECHA_EXPIRACION = os.time({year = 2026, month = 4, day = 21, hour = 18, min = 0})
local DURACION_SESION = 7200 
local startTime = os.time()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- VERIFICAR EXPIRACIÓN
if os.time() > FECHA_EXPIRACION then
    LocalPlayer:Kick("ESTE SCRIPT YA EXPIRO")
    return
end

-- 1. ANUNCIO RAINBOW LARDYLAN HUB
local function showIntro()
    local screenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
    local label = Instance.new("TextLabel", screenGui)
    label.Size = UDim2.new(1, 0, 0.1, 0)
    label.Position = UDim2.new(0, 0, 0.1, 0)
    label.BackgroundTransparency = 1
    label.Text = "LARDYLAN HUB"
    label.TextSize = 40
    label.Font = Enum.Font.LuckiestGuy
    
    local conn
    conn = RunService.RenderStepped:Connect(function()
        label.TextColor3 = Color3.fromHSV(tick() % 5 / 5, 1, 1)
    end)

    task.delay(5, function()
        conn:Disconnect()
        TweenService:Create(label, TweenInfo.new(1), {TextTransparency = 1}):Play()
        task.wait(1.1)
        screenGui:Destroy()
    end)
end
showIntro()

-- 2. CONFIGURACIÓN DEL FOV (Círculo)
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.Color = Color3.fromRGB(0, 255, 255)
FOVCircle.Radius = 150
FOVCircle.Filled = false
FOVCircle.Visible = true

-- 3. ESP Y ANTENAS AZULES
local function applyESP(plr)
    local function createVisuals()
        if plr ~= LocalPlayer and plr.Character then
            -- Borrar ESP previo si existe
            if plr.Character:FindFirstChild("LardylanESP") then plr.Character.LardylanESP:Destroy() end
            if plr.Character:FindFirstChild("AntenaAzul") then plr.Character.AntenaAzul:Destroy() end

            -- Highlight (Cuerpo Azul)
            local highlight = Instance.new("Highlight")
            highlight.Name = "LardylanESP"
            highlight.FillColor = Color3.fromRGB(0, 0, 255)
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            highlight.FillTransparency = 0.5
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = plr.Character
            
            -- Antena (Palo Gigante)
            local antenna = Instance.new("Part")
            antenna.Name = "AntenaAzul"
            antenna.Size = Vector3.new(0.5, 30, 0.5)
            antenna.Color = Color3.fromRGB(0, 0, 255)
            antenna.Material = Enum.Material.Neon
            antenna.CanCollide = false
            antenna.Parent = plr.Character
            
            local weld = Instance.new("Weld")
            weld.Part0 = plr.Character:WaitForChild("Head")
            weld.Part1 = antenna
            weld.C0 = CFrame.new(0, 15, 0)
            weld.Parent = antenna
        end
    end
    plr.CharacterAdded:Connect(function() task.wait(1) createVisuals() end)
    createVisuals()
end

for _, p in pairs(Players:GetPlayers()) do applyESP(p) end
Players.PlayerAdded:Connect(applyESP)

-- 4. LÓGICA AIMBOT
local function getTarget()
    local target = nil
    local shortestDist = FOVCircle.Radius

    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") then
            local head = v.Character.Head
            local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - FOVCircle.Position).Magnitude
                if dist < shortestDist then
                    target = head
                    shortestDist = dist
                end
            end
        end
    end
    return target
end

-- 5. BUCLE PRINCIPAL
RunService.RenderStepped:Connect(function()
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    local targetHead = getTarget()
    if targetHead then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetHead.Position)
    end
    
    if os.time() - startTime >= DURACION_SESION then
        LocalPlayer:Kick("SESION FINALIZADA: 2 HORAS CUMPLIDAS")
    end
end)
        rainbowConn:Disconnect()
        TweenService:Create(label, TweenInfo.new(1), {TextTransparency = 1}):Play()
        task.wait(1)
        screenGui:Destroy()
    end)
end
showRainbowIntro()

local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.Color = Color3.fromRGB(0, 255, 255)
FOVCircle.Radius = 150
FOVCircle.Filled = false
FOVCircle.Visible = true

local function applyESP(plr)
    local function createVisuals()
        if plr.Character then
            local highlight = Instance.new("Highlight")
            highlight.Name = "LardylanESP"
            highlight.FillColor = Color3.fromRGB(0, 0, 255)
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            highlight.FillTransparency = 0.4
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = plr.Character
            
            local antenna = Instance.new("Part")
            antenna.Size = Vector3.new(0.3, 25, 0.3)
            antenna.Color = Color3.fromRGB(0, 0, 255)
            antenna.CanCollide = false
            antenna.Material = Enum.Material.Neon
            antenna.Parent = plr.Character
            
            local weld = Instance.new("Weld")
            weld.Part0 = plr.Character:WaitForChild("Head")
            weld.Part1 = antenna
            weld.C0 = CFrame.new(0, 12.5, 0)
            weld.Parent = antenna
        end
    end
    plr.CharacterAdded:Connect(createVisuals)
    createVisuals()
end

for _, p in pairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then applyESP(p) end
end
Players.PlayerAdded:Connect(applyESP)

local function getTarget()
    local target = nil
    local shortestDist = FOVCircle.Radius

    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") then
            local pos, onScreen = Camera:WorldToViewportPoint(v.Character.Head.Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - FOVCircle.Position).Magnitude
                if dist < shortestDist then
                    target = v.Character.Head
                    shortestDist = dist
                end
            end
        end
    end
    return target
end

local startTime = os.time()

RunService.RenderStepped:Connect(function()
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    local head = getTarget()
    if head then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, head.Position)
    end
    if os.time() - startTime >= DURACION_SESION then
        LocalPlayer:Kick("SESION FINALIZADA: 2 HORAS CUMPLIDAS")
    end
end)
        if plr.Character then
            -- ESP Cuerpo Azul (Highlight)
            local highlight = Instance.new("Highlight")
            highlight.Name = "LardylanESP"
            highlight.FillColor = Color3.fromRGB(0, 0, 255)
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            highlight.FillTransparency = 0.4
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = plr.Character
            
            -- Antena Azul (Palo gigante)
            local antenna = Instance.new("Part")
            antenna.Size = Vector3.new(0.3, 25, 0.3)
            antenna.Color = Color3.fromRGB(0, 0, 255)
            antenna.CanCollide = false
            antenna.Material = Enum.Material.Neon
            antenna.Parent = plr.Character
            
            local weld = Instance.new("Weld")
            weld.Part0 = plr.Character:WaitForChild("Head")
            weld.Part1 = antenna
            weld.C0 = CFrame.new(0, 12.5, 0)
            weld.Parent = antenna
        end
    end
    plr.CharacterAdded:Connect(createVisuals)
    createVisuals()
end

for _, p in pairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then applyESP(p) end
end
Players.PlayerAdded:Connect(applyESP)

-- 6. LÓGICA AIMBOT (Cabeza)
local function getTarget()
    local target = nil
    local shortestDist = FOVCircle.Radius

    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") then
            local pos, onScreen = Camera:WorldToViewportPoint(v.Character.Head.Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - FOVCircle.Position).Magnitude
                if dist < shortestDist then
                    target = v.Character.Head
                    shortestDist = dist
                end
            end
        end
    end
    return target
end

-- 7. BUCLE PRINCIPAL (Aimbot + Contador de 2h)
local startTime = os.time()

RunService.RenderStepped:Connect(function()
    -- Centrar FOV
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    -- Apuntar si hay objetivo
    local head = getTarget()
    if head then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, head.Position)
    end
    
    -- Verificar tiempo de sesión (2 horas)
    if os.time() - startTime >= DURACION_SESION then
        LocalPlayer:Kick("SESION FINALIZADA: 2 HORAS CUMPLIDAS")
    end
end)

print("Lardylan Hub cargado correctamente.")
            weld.Part0 = plr.Character:WaitForChild("Head")
            weld.Part1 = antenna
            weld.C0 = CFrame.new(0, 10, 0)
            weld.Parent = antenna
        end
    end
    plr.CharacterAdded:Connect(apply)
    apply()
end

for _, p in pairs(game.Players:GetPlayers()) do
    if p ~= Player then createESP(p) end
end
game.Players.PlayerAdded:Connect(createESP)

-- 4. BUCLE DE EJECUCIÓN Y CONTADOR
local startTime = os.time()

RunService.RenderStepped:Connect(function()
    -- Actualizar posición FOV por si cambia resolución
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    -- Ejecutar Aimbot
    local head = getClosestPlayerInFOV()
    if head then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, head.Position)
    end
    
    -- Contador de 2 horas
    local elapsed = os.time() - startTime
    if elapsed >= DURACION_SESION then
        game.Players.LocalPlayer:Kick("SESION FINALIZADA: 2 HORAS CUMPLIDAS")
    end
end)
char.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame
    end
end)

speedBtn.Parent = mainFrame
speedBtn.Text = "VELOCIDAD: OFF"
speedBtn.Size = UDim2.new(0.9, 0, 0.12, 0)
speedBtn.Position = UDim2.new(0.05, 0, 0.65, 0)
speedBtn.BackgroundColor3 = Color3.fromRGB(0, 130, 0)
speedBtn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", speedBtn)
speedBtn.MouseButton1Click:Connect(function()
    speedActive = not speedActive
    local char = getChar()
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then
        hum.WalkSpeed = speedActive and 100 or 16
        speedBtn.Text = speedActive and "VELOCIDAD: ON" or "VELOCIDAD: OFF"
    end
end)

invisBtn.Parent = mainFrame
invisBtn.Text = "INVISIBILIDAD"
invisBtn.Size = UDim2.new(0.9, 0, 0.12, 0)
invisBtn.Position = UDim2.new(0.05, 0, 0.80, 0)
invisBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
invisBtn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", invisBtn)
invisBtn.MouseButton1Click:Connect(function()
    local char = getChar()
    for _, obj in pairs(char:GetDescendants()) do
        if obj:IsA("BasePart") or obj:IsA("Decal") then
            obj.Transparency = (obj.Transparency == 0 and 1 or 0)
        end
    end
end)

-- LISTA DE JUGADORES (CORREGIDA PARA DELTA)
playerListFrame.Parent = mainFrame
playerListFrame.Size = UDim2.new(1, 0, 1, 0) -- Tamaño completo
playerListFrame.Position = UDim2.new(1.05, 0, 0, 0) -- Aparece al lado
playerListFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
playerListFrame.Visible = false
playerListFrame.ScrollBarThickness = 5
Instance.new("UICorner", playerListFrame)

local listLayout = Instance.new("UIListLayout", playerListFrame)
listLayout.Padding = UDim.new(0, 5)

selectedLabel.MouseButton1Click:Connect(function()
    playerListFrame.Visible = not playerListFrame.Visible
    if playerListFrame.Visible then
        for _, v in pairs(playerListFrame:GetChildren()) do 
            if v:IsA("TextButton") then v:Destroy() end 
        end
        local players = game.Players:GetPlayers()
        playerListFrame.CanvasSize = UDim2.new(0, 0, 0, #players * 35) -- AJUSTA EL TAMAÑO DE LA HOJA
        
        for _, p in pairs(players) do
            if p ~= player then
                local b = Instance.new("TextButton", playerListFrame)
                b.Size = UDim2.new(0.9, 0, 0, 30)
                b.Position = UDim2.new(0.05, 0, 0, 0)
                b.Text = p.Name
                b.BackgroundColor3 = Color3.fromRGB(60, 60, 60) -- Color más visible
                b.TextColor3 = Color3.new(1, 1, 1)
                b.Font = Enum.Font.SourceSansBold
                Instance.new("UICorner", b)
                
                b.MouseButton1Click:Connect(function()
                    targetPlayer = p
                    selectedLabel.Text = "Listo: " .. p.Name
                    playerListFrame.Visible = false
                end)
            end
        end
    end
end)
tpPlayerBtn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", tpPlayerBtn)
tpPlayerBtn.MouseButton1Click:Connect(function()
    local char = getChar()
    if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
        char.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame
    end
end)

speedBtn.Parent = mainFrame
speedBtn.Text = "VELOCIDAD: OFF"
speedBtn.Size = UDim2.new(0.9, 0, 0.12, 0)
speedBtn.Position = UDim2.new(0.05, 0, 0.65, 0)
speedBtn.BackgroundColor3 = Color3.fromRGB(0, 130, 0)
speedBtn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", speedBtn)
speedBtn.MouseButton1Click:Connect(function()
    speedActive = not speedActive
    local char = getChar()
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then
        hum.WalkSpeed = speedActive and 100 or 16
        speedBtn.Text = speedActive and "VELOCIDAD: ON" or "VELOCIDAD: OFF"
    end
end)

invisBtn.Parent = mainFrame
invisBtn.Text = "INVISIBILIDAD"
invisBtn.Size = UDim2.new(0.9, 0, 0.12, 0)
invisBtn.Position = UDim2.new(0.05, 0, 0.80, 0)
invisBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
invisBtn.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", invisBtn)
invisBtn.MouseButton1Click:Connect(function()
    local char = getChar()
    for _, obj in pairs(char:GetDescendants()) do
        if obj:IsA("BasePart") or obj:IsA("Decal") then
            obj.Transparency = (obj.Transparency == 0 and 1 or 0)
        end
    end
end)

playerListFrame.Parent = mainFrame
playerListFrame.Size = UDim2.new(1, 0, 0.8, 0)
playerListFrame.Position = UDim2.new(1.05, 0, 0.1, 0)
playerListFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
playerListFrame.Visible = false
Instance.new("UICorner", playerListFrame)

local listLayout = Instance.new("UIListLayout", playerListFrame)

selectedLabel.MouseButton1Click:Connect(function()
    playerListFrame.Visible = not playerListFrame.Visible
    if playerListFrame.Visible then
        for _, v in pairs(playerListFrame:GetChildren()) do 
            if v:IsA("TextButton") then v:Destroy() end 
        end
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= player then
                local b = Instance.new("TextButton", playerListFrame)
                b.Size = UDim2.new(1, 0, 0, 30)
                b.Text = p.Name
                b.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                b.TextColor3 = Color3.new(1, 1, 1)
                b.MouseButton1Click:Connect(function()
                    targetPlayer = p
                    selectedLabel.Text = p.Name
                    playerListFrame.Visible = false
                end)
            end
        end
    end
end)
playerListFrame.Position = UDim2.new(1.1, 0, 0, 0)
playerListFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
playerListFrame.Visible = false
playerListFrame.CanvasSize = UDim2.new(0, 0, 0, 0)

local function updatePlayerList()
    playerListFrame:ClearAllChildren()
    local layout = Instance.new("UIListLayout", playerListFrame)
    local count = 0
    for _, p in pairs(game.Players:GetPlayers()) do
        if p ~= player then
            count = count + 1
            local btn = Instance.new("TextButton", playerListFrame)
            btn.Size = UDim2.new(1, 0, 0, 30)
            btn.Text = p.Name
            btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
            btn.TextColor3 = Color3.new(1, 1, 1)
            btn.MouseButton1Click:Connect(function()
                targetPlayer = p
                selectedLabel.Text = "Destino: " .. p.Name
                playerListFrame.Visible = false
            end)
        end
    end
    playerListFrame.CanvasSize = UDim2.new(0, 0, 0, count * 30)
end

selectedLabel.MouseButton1Click:Connect(function()
    playerListFrame.Visible = not playerListFrame.Visible
    if playerListFrame.Visible then updatePlayerList() end
end)

-- BOTÓN 2: Teleport al Jugador (Perfecto para soltar la bomba)
tpPlayerBtn.Parent = mainFrame
tpPlayerBtn.Text = "TP AL JUGADOR (BOMBA)"
tpPlayerBtn.Size = UDim2.new(0.9, 0, 0.25, 0)
tpPlayerBtn.Position = UDim2.new(0.05, 0, 0.65, 0)
tpPlayerBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0) -- Rojo para la bomba
tpPlayerBtn.TextColor3 = Color3.new(1, 1, 1)
tpPlayerBtn.Font = Enum.Font.SourceSansBold
tpPlayerBtn.TextSize = 11
Instance.new("UICorner", tpPlayerBtn)

tpPlayerBtn.MouseButton1Click:Connect(function()
    if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
        player.Character.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame
    end
end)
local JUEGO_PERMITIDO_ID = 0 
local FECHA_EXPIRACION = os.time({year = 2026, month = 4, day = 21, hour = 18, min = 0})
local DURACION_SESION = 7200 

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

if game.PlaceId ~= JUEGO_PERMITIDO_ID and JUEGO_PERMITIDO_ID ~= 0 then
    LocalPlayer:Kick("ESTE SCRIPT NO ES PARA ESTE JUEGO")
    return
end

if os.time() > FECHA_EXPIRACION then
    LocalPlayer:Kick("ESTE SCRIPT YA EXPIRO")
    return
end

local function showRainbowIntro()
    local screenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
    local label = Instance.new("TextLabel", screenGui)
    label.Size = UDim2.new(1, 0, 0.1, 0)
    label.Position = UDim2.new(0, 0, 0.1, 0)
    label.BackgroundTransparency = 1
    label.Text = "LARDYLAN HUB"
    label.TextScaled = true
    label.Font = Enum.Font.LuckiestGuy
    
    local rainbowConn
    rainbowConn = RunService.RenderStepped:Connect(function()
        label.TextColor3 = Color3.fromHSV(tick() % 5 / 5, 1, 1)
    end)

    task.delay(5, function()
        rainbowConn:Disconnect()
        TweenService:Create(label, TweenInfo.new(1), {TextTransparency = 1}):Play()
        task.wait(1)
        screenGui:Destroy()
    end)
end
showRainbowIntro()

local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.Color = Color3.fromRGB(0, 255, 255)
FOVCircle.Radius = 150
FOVCircle.Filled = false
FOVCircle.Visible = true

local function applyESP(plr)
    local function createVisuals()
        if plr.Character then
            local highlight = Instance.new("Highlight")
            highlight.Name = "LardylanESP"
            highlight.FillColor = Color3.fromRGB(0, 0, 255)
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            highlight.FillTransparency = 0.4
            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            highlight.Parent = plr.Character
            
            local antenna = Instance.new("Part")
            antenna.Size = Vector3.new(0.3, 25, 0.3)
            antenna.Color = Color3.fromRGB(0, 0, 255)
            antenna.CanCollide = false
            antenna.Material = Enum.Material.Neon
            antenna.Parent = plr.Character
            
            local weld = Instance.new("Weld")
            weld.Part0 = plr.Character:WaitForChild("Head")
            weld.Part1 = antenna
            weld.C0 = CFrame.new(0, 12.5, 0)
            weld.Parent = antenna
        end
    end
    plr.CharacterAdded:Connect(createVisuals)
    createVisuals()
end

for _, p in pairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then applyESP(p) end
end
Players.PlayerAdded:Connect(applyESP)

local function getTarget()
    local target = nil
    local shortestDist = FOVCircle.Radius

    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") then
            local pos, onScreen = Camera:WorldToViewportPoint(v.Character.Head.Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - FOVCircle.Position).Magnitude
                if dist < shortestDist then
                    target = v.Character.Head
                    shortestDist = dist
                end
            end
        end
    end
    return target
end

local startTime = os.time()

RunService.RenderStepped:Connect(function()
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    local head = getTarget()
    if head then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, head.Position)
    end
    if os.time() - startTime >= DURACION_SESION then
        LocalPlayer:Kick("SESION FINALIZADA: 2 HORAS CUMPLIDAS")
    end
end)
